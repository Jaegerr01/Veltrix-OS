'use client';

import { PlaceholderModule } from '@/components/ds';

export default function ClientsPage() {
  return (
    <PlaceholderModule
      icon="briefcase"
      eyebrow="PIPELINE"
      title="Clients"
      blurb="Your book of business — health scores, renewals, and account owners."
    />
  );
}
