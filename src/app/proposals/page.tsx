'use client';

import { PlaceholderModule } from '@/components/ds';

export default function ProposalsPage() {
  return (
    <PlaceholderModule
      icon="doc"
      eyebrow="PIPELINE"
      title="Proposals"
      blurb="Draft, send, and track proposals with live open + signature status."
    />
  );
}
